import { AuthButton } from "@/components/auth-button"

export default function LoginPage() {
  return (
    <main>
      <h1>Authentication is required to access this resource</h1>

      <AuthButton />
    </main>
  )
}
